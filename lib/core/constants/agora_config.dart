/// Agora RTC App ID (public). RTC tokens must be generated on the backend.
///
/// Supported platforms: **iOS** and **Android** (`agora_rtc_engine`).
/// Platform setup: `android/app/src/main/AndroidManifest.xml`,
/// `ios/Runner/Info.plist`, `ios/Podfile` (permission_handler macros).
class AgoraConfig {
  AgoraConfig._();

  static const String appId = '5ad2726cd97d42f8a4c9ccdcd00b910d';
}
